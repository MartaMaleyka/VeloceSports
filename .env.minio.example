# Credenciales MinIO (desarrollo local) — copiar a .env.minio junto a docker-compose.yml
# Consola: http://localhost:9101  ·  API: http://127.0.0.1:9100
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin_change_me
MINIO_BUCKET=squadveloce-players
