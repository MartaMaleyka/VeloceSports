---
import BaseLayout from '../../layouts/BaseLayout.astro';
import DashboardShell from '../../components/layout/DashboardShell';
import { getSession } from '../../lib/session';
import { redirectUnlessRole } from '../../lib/page-access';
import { t } from '@velocesport/i18n';

const session = getSession(Astro.cookies);
const accessDenied = redirectUnlessRole(session, 'coach');
if (accessDenied) return Astro.redirect(accessDenied);

const locale = Astro.locals.locale;
---

<BaseLayout
  locale={locale}
  title={`${t('dashboard.coach.home.title', locale)} — ${t('common.appName', locale)}`}
  description={t('meta.appDescription', locale)}
>
  <DashboardShell
    client:load
    initialLocale={locale}
    role={session.role}
    roles={session.roles}
    activeNavId="home"
    contentKey="coach"
    pageId="home"
    pageTitle={t('dashboard.coach.home.title', locale)}
    pageDescription={t('dashboard.coach.home.description', locale)}
    hideWelcome
    headerSectionAccent="brand"
  />
</BaseLayout>
