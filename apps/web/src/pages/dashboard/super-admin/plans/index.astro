---
import BaseLayout from '../../../../layouts/BaseLayout.astro';
import DashboardShell from '../../../../components/layout/DashboardShell';
import { getSession } from '../../../../lib/session';
import { t } from '@velocesport/i18n';

const session = getSession(Astro.cookies);
if (!session || session.role !== 'super_admin') {
  return Astro.redirect('/login');
}

const locale = Astro.locals.locale;
---

<BaseLayout
  locale={locale}
  title={`${t('platform.plans.title', locale)} — ${t('common.appName', locale)}`}
  description={t('platform.plans.description', locale)}
>
  <DashboardShell
    client:load
    initialLocale={locale}
    role={session.role}
    activeNavId="plans"
    contentKey="superAdmin"
    pageTitle={t('platform.plans.title', locale)}
    pageDescription={t('platform.plans.description', locale)}
    hideWelcome
    pageId="plans-list"
  />
</BaseLayout>
